package fi.joniaromaa.cheatdetectionmicroservice.services.mixin.platform.container;

/*public class ContainerHandleCheatDetection extends ContainerHandleVirtual
{
    public ContainerHandleCheatDetection()
    {
		super("CheatDetection");
	}

	public void addResource(String name, Path path)
    {
		System.out.println("Add resoruce: " + name);
        //this.add(new Resource(name, path));
    }
    
    public void addResources(List<Entry<String, Path>> resources)
    {
        for (Entry<String, Path> resource : resources)
        {
            this.addResource(resource.getKey(), resource.getValue());
        }
    }
    
    @Override
    public String toString()
    {
        return String.format("CheatDetection Root Container(%x)", this.hashCode());
    }
}*/
